<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>O Caça-Palavras do Nosso Amor 💖</title>
    <style>
        :root {
            --cor-primaria: #ff4d6d;
            --cor-secundaria: #ff85a1;
            --cor-fundo: #fff0f3;
            --cor-texto: #590d22;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--cor-fundo);
            color: var(--cor-texto);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(255, 77, 109, 0.15);
            max-width: 900px;
            width: 100%;
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .titulo-secao {
            width: 100%;
            text-align: center;
            margin-bottom: 10px;
            color: var(--cor-primaria);
        }

        /* Tabuleiro do Jogo */
        #grid {
            display: grid;
            grid-template-columns: repeat(10, 40px);
            grid-template-rows: repeat(10, 40px);
            gap: 5px;
            background: #fff0f3;
            padding: 10px;
            border-radius: 10px;
            user-select: none;
        }

        .cell {
            background: white;
            border: 1px solid #ffccd5;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
            cursor: pointer;
        }

        .cell.selected {
            background: var(--cor-secundaria);
            color: white;
        }

        .cell.found {
            background: var(--cor-primaria);
            color: white;
        }

        /* Lista de Palavras */
        .sidebar {
            flex: 1;
            min-width: 250px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .lista-palavras {
            list-style: none;
        }

        .lista-palavras li {
            padding: 8px 12px;
            margin-bottom: 5px;
            background: #f8f9fa;
            border-radius: 5px;
            border-left: 4px solid #ffccd5;
            font-weight: 600;
        }

        .lista-palavras li.achada {
            background: #d4edda;
            border-left-color: #28a745;
            color: #155724;
            text-decoration: line-through;
            opacity: 0.7;
        }

        .dicas-box {
            background: #fff8f9;
            padding: 15px;
            border-radius: 10px;
            border: 1px dashed var(--cor-secundaria);
            font-size: 14px;
        }

        .dicas-box h3 {
            margin-bottom: 8px;
            color: var(--cor-primaria);
        }

        /* Tela Final Oculta */
        .tela-final {
            display: none;
            text-align: center;
        }

        .tela-final h1 {
            font-size: 50px;
            color: var(--cor-primaria);
            margin-bottom: 20px;
        }

        .coracao-decoracao {
            font-size: 80px;
            color: var(--cor-primaria);
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="container" id="jogo-container">
        <div class="titulo-secao">
            <h2>Encontre as palavras do nosso amor ❤️</h2>
            <p>Clique nas letras para selecionar a palavra!</p>
        </div>

        <div id="grid"></div>

        <div class="sidebar">
            <h3>Palavras para achar:</h3>
            <ul class="lista-palavras" id="lista"></ul>

            <div class="dicas-box">
                <h3>💡 Dicas das enigmáticas:</h3>
                <p><strong>Dica 1:</strong> O que nós mais fazemos quando estamos juntos? (7 letras)</p>
                <p style="margin-top: 5px;"><strong>Dica 2:</strong> O sentimento que define quando estou do seu lado. (3 letras)</p>
            </div>
        </div>
    </div>

    <div class="tela-final" id="tela-final">
        <h1>EU AMO VOCÊ! 🥰</h1>
        <p style="font-size: 20px;">Você encontrou todas as palavras e desvendou o segredo do meu coração!</p>
        <div class="coracao-decoracao">❤️</div>
    </div>

    <script>
        // Tabuleiro fixo pré-gerado para EVITAR qualquer travamento de lógica aleatória
        const mapaLetras = [
            ['C','A','R','I','N','H','O','X','Y','Z'],
            ['A','X','X','X','X','X','X','X','X','P'],
            ['F','U','T','U','R','O','X','X','X','A'],
            ['X','X','X','X','X','X','X','X','X','Z'],
            ['C','O','N','F','I','A','N','C','A','X'],
            ['X','X','X','X','X','X','X','X','X','X'],
            ['A','V','E','N','T','U','R','A','X','X'],
            ['X','X','X','X','X','X','X','X','X','X'],
            ['R','I','S','A','D','A','S','X','X','X'],
            ['X','X','X','X','X','X','X','X','X','X']
        ];

        const PALAVRAS = ["CARINHO", "FUTURO", "CONFIANCA", "AVENTURA", "RISADAS", "PAZ"];
        let palavrasAchadas = [];
        let letrasSelecionadas = [];

        // Preencher letras aleatórias onde tem 'X'
        const alfabeto = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        for (let l = 0; l < 10; l++) {
            for (let c = 0; c < 10; c++) {
                if (mapaLetras[l][c] === 'X') {
                    mapaLetras[l][c] = alfabeto[Math.floor(Math.random() * alfabeto.length)];
                }
            }
        }

        // Criar a lista na barra lateral
        const listaUI = document.getElementById('lista');
        PALAVRAS.forEach(p => {
            let item = document.createElement('li');
            item.id = "word-" + p;
            item.innerText = (p === "RISADAS" || p === "PAZ") ? "❓ PALAVRA SECRETA (" + p.length + " letras)" : p;
            listaUI.appendChild(item);
        });

        // Desenhar o tabuleiro
        const gridItem = document.getElementById('grid');
        for (let l = 0; l < 10; l++) {
            for (let c = 0; c < 10; c++) {
                let celula = document.createElement('div');
                celula.className = 'cell';
                celula.innerText = mapaLetras[l][c];
                
                celula.addEventListener('click', function() {
                    if (this.classList.contains('found')) return;

                    if (this.classList.contains('selected')) {
                        this.classList.remove('selected');
                        letrasSelecionadas = letrasSelecionadas.filter(item => item !== celula);
                    } else {
                        this.classList.add('selected');
                        letrasSelecionadas.push(celula);
                    }

                    verificarPalavra();
                });

                gridItem.appendChild(celula);
            }
        }

        function verificarPalavra() {
            let palavraFormada = letrasSelecionadas.map(c => c.innerText).join('');
            let palavraInversa = palavraFormada.split('').reverse().join('');

            let palavraCerta = "";
            if (PALAVRAS.includes(palavraFormada)) palavraCerta = palavraFormada;
            else if (PALAVRAS.includes(palavraInversa)) palavraCerta = palavraInversa;

            if (palavraCerta && !palavrasAchadas.includes(palavraCerta)) {
                palavrasAchadas.push(palavraCerta);
                letrasSelecionadas.forEach(c => {
                    c.classList.remove('selected');
                    c.classList.add('found');
                });
                letrasSelecionadas = [];

                let itemLista = document.getElementById("word-" + palavraCerta);
                itemLista.className = "achada";
                itemLista.innerText = palavraCerta + " ✓";

                if (palavrasAchadas.length === PALAVRAS.length) {
                    setTimeout(() => {
                        document.getElementById('jogo-container').style.display = 'none';
                        document.getElementById('tela-final').style.display = 'block';
                    }, 500);
                }
            }
        }
    </script>
</body>
</html>
